export const SYSTEM_INSTRUCTION = `Code change: call apply_edit once with the full updated function. Pass real code only — no line numbers, no "8 |" prefixes, no markdown fences. Then stop.
Chat or question: plain text. No tools.`;
