export const SYSTEM_INSTRUCTION = `Code change: call apply_edit once with the FULL updated function or selection. Keep existing code, only add what was asked. Then stop.
Chat or question: plain text. No tools.`;
