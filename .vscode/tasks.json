{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "extension: build",
      "type": "shell",
      "command": "bun run build",
      "options": {
        "cwd": "${workspaceFolder}/apps/extension"
      },
      "group": "build",
      "problemMatcher": ["$tsc"]
    }
  ]
}
